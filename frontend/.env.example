# ClassConnect Frontend Environment Variables

# The Base URL of the deployed Express backend API (include /api path)
VITE_API_BASE_URL=http://localhost:5000/api

# The Base URL of the deployed Socket.IO server (usually same host as API, without /api path)
VITE_SOCKET_URL=http://localhost:5000
